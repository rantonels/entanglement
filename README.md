il sorgente della libreria è `entanglement.pyx` dove le funzioni sono semidocumentate

Per compilare

```
python setup.py build_ext --inplace
```

poi si può usare tranquillamente con

```
import entanglement as ee
```

